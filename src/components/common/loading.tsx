import React from "react";
import style from "@src/src/components/common/loading.module.scss";

const Loading: React.FC = () => {
  return <div className={style.circleLoading}></div>;
};

export default Loading;
